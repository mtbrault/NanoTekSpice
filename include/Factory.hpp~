//
// EPITECH PROJECT, 2018
// nanotekspice
// File description:
// factory class
//

#ifndef FACTORY_HPP_
# define FACTORY_HPP_

#include <iostream>
#include "IComponent.hpp"

class	Factory
{
	Factory();
	~Factory();

	std::unique_ptr<IComponent> createComponent(const std::string &,
						    const std::string &);
	std::unique_ptr<IComponent> create2716(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4001(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4008(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4011(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4013(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4017(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4030(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4040(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4069(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4071(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4081(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4094(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4514(const std::string &)
		const noexcept;
	std::unique_ptr<IComponent> create4801(const std::string &)
		const noexcept;
};

#endif
